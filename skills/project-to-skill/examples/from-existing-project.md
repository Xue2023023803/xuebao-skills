# Example Prompt

Use `$project-to-skill` to turn this finished local RAG project into a reusable Codex skill repo. Keep the reusable workflow, references, and constraints, but remove machine-specific paths, logs, caches, and model files.
