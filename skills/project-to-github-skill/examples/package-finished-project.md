# Example Prompt

Use `$project-to-github-skill` to turn this finished project into a GitHub-ready Codex skill repository with a clean `skills/` directory, bilingual README files, an install script, and publishable metadata.
